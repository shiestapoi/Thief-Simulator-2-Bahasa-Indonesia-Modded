# ============================================================================
# INSTALLATION INSTRUCTIONS:
# 1. Install main dependencies: pip install -r requirements.txt
# 2. Install PyTorch XPU: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/xpu
# 3. (Optional) Install Intel extensions: pip install intel-extension-for-pytorch mkl
# ============================================================================

# Core dependencies for translation system
transformers>=4.21.0
tqdm>=4.64.0

# Additional tokenization and processing
sentencepiece>=0.1.97
sacremoses>=0.0.53
tokenizers>=0.13.0

# Model and data handling
protobuf>=3.20.0
numpy>=1.21.0

# ============================================================================
# PyTorch XPU Installation (run separately):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/xpu
# ============================================================================

# ============================================================================
# Optional Intel XPU optimizations (run separately):
# pip install intel-extension-for-pytorch>=2.0.0 mkl>=2023.0.0
# ============================================================================

# Standard library modules (already included in Python):
# json, re, argparse, logging, os, sys, time, functools, typing, dataclasses, pathlib

# File compatibility:
# - temp.py: Uses facebook/nllb-200-distilled-600M model with AutoModelForSeq2SeqLM
# - translate.py: Uses Helsinki-NLP/opus-mt-en-id model with MarianMTModel
# Both files are compatible with the above dependencies